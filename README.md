To get this demo running, do the following from you command line:

```
git clone git@github.com:Jpunt/workshop.git
cd workshop
bundle install
rails server
```

It will be running on [http://localhost:3000](http://localhost:3000) :)
